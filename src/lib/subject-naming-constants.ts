/** Shared thresholds for slate auto-apply and client-side naming hints. */
export const SUBJECT_SLATE_APPLY_MIN_CONFIDENCE = 0.62;
export const SUBJECT_MATCH_MIN_CONFIDENCE_DEFAULT = 0.8;
/** Below this (before consensus boost), escalate to a stronger vision model. */
export const SUBJECT_SLATE_ESCALATION_MAX_CONFIDENCE = 0.78;
